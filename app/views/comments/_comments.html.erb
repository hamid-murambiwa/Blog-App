<%= form_with url: user_post_comments_path, scope: :comment do |form| %>
<div class="comment-c">
	<%= form.text_area :text, placeholder: "Add your comment...", rows: 14, required: true %>
	<%= form.submit "Comment", class: "submit-btn" %>
  </div>
<% end %>